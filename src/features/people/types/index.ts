export type Person = {
  id: string;
  name: string;
  email: string;
  phone: string;
  cpf: string;
  avatarUrl: string;
  movies: string[];
  songs: string[];
};

export type Avatar = {
  id: string;
  url: string;
};
